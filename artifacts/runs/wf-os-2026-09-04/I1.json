{
  "taskId": "I1",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "COMMITTED",
  "evidence": "PR #36 (https://github.com/EZiLHQ/ezil-os/pull/36) MERGED at 2026-09-04T21:34:24Z, squash commit b04f746: CONTRIBUTING.md +165/README.md +10-1/GOVERNANCE.md +5/.github/PULL_REQUEST_TEMPLATE.md +10/.github/ISSUE_TEMPLATE/{task,app_proposal}.yml new. FOLLOW-UP REQUIRED AND OPENED: PR #35 (row I5's tools/triage.ts) merged concurrently with #36 and hardcodes CONTRIBUTING.md#pr-size, #how-to-send-a-pr, #reading-ci, #sign-off-dco in its posted guidance text -- measured on origin/main post-merge via `grep -n \"CONTRIBUTING.md#\" tools/triage.ts`. #36's own CONTRIBUTING.md (built from this row's original brief, which specified #how-to-send-a-pull-request and a heading \"How big a pull request should be\") did not match two of those four anchors -- a real, landed dead-link pair, not a hypothetical, caught by advisor review of this row's own transcript after #36 had already auto-merged (checks went green faster than the fix commit could be added). A mid-task coordinator message also flagged this same mismatch independently (naming the exact required heading text) before the advisor review completed; both agree. PR #38 (https://github.com/EZiLHQ/ezil-os/pull/38, branch fix/I1-anchor-contract, auto-merge squash ARMED as of 2026-09-04T21:57:50Z, not yet merged as of this writing) renames the two headings (`## How big a pull request should be` -> `## PR size`, `## How to send a pull request` -> `## How to send a PR`) so their GitHub slugs match tools/triage.ts verbatim; #sign-off-dco and #reading-ci needed no change. PR #38 ALSO fixes two review findings caught in the same pass, before any of #36's content had a second reader: (a) tools/test.sh's header names exactly THREE rules -- absent summary, unreadable failure count, any non-zero exit passed through untouched -- and #36's \"A skip is not a pass\" section had replaced the real rule 3 with skip-naming (a real behaviour of that file, but a separate section of it, 'THE SKIP HAZARD', not one of the three named rules); corrected to quote the real rule 3 (re-read via `sed -n '28,41p' tools/test.sh`) and describe skip-naming as additional to the three. (b) the Discussions section's app-proposal link used a raw-YAML blob path instead of this repository's own `issues/new?template=...` convention (already used by README.md for bug_report.yml) -- switched to match. VERIFY, run against #38's branch before opening it: `grep -n '^## ' CONTRIBUTING.md` shows PR size/How to send a PR/Reading CI/Sign-off (DCO) all present; `python3 -c \"import yaml,sys; [yaml.safe_load(open(f)) for f in sys.argv[1:]]\" .github/ISSUE_TEMPLATE/task.yml .github/ISSUE_TEMPLATE/app_proposal.yml .github/ISSUE_TEMPLATE/config.yml` -> all three parse; `git diff --stat origin/main..HEAD` on the fix branch shows exactly 4 files, 17 insertions/13 deletions -- no unrelated drift. REQUIRED-CONTEXT COMPARISON FOR #36 (both sides, verbatim, 15/15 match, done before #36 was opened): live `gh api repos/EZiLHQ/ezil-os/rulesets/22265548 --jq '.rules[]|select(.type==\"required_status_checks\")|.parameters.required_status_checks[].context'` diffed byte-for-byte against the 15 backtick-quoted lines in CONTRIBUTING.md's Reading CI section -- empty diff. ci.yml:64-72 fork/GHCR paragraph quoted verbatim, re-checked after rebasing onto main post-I0c. docs/CONTRIBUTING-APPS.md (linked from README.md and CONTRIBUTING.md) does NOT exist yet in this tree -- deliberate per the brief ('row I2 is writing it in parallel -- link it anyway'); resolves once I2 merges. Labels verified live via `gh label list -R EZiLHQ/ezil-os`, not assumed: every label this row's content references already exists (good first issue, help wanted, size/XS..XL, needs-triage, blocked, prereq-missing, review:claude, dependencies, all ten area labels) -- the plan's 'supervisor gh label create' step is a no-op for this row. Discussions is ENABLED on the repo (`gh api repos/EZiLHQ/ezil-os --jq '.has_discussions'` -> true, checked during review, not assumed) -- only the five categories (Announcements, Q&A, Ideas, Show and tell, Apps) remain a founder dashboard step, since GitHub exposes no public API to create Discussion categories; stated as such in CONTRIBUTING's Discussions section. DEVIATION FROM THE PLAN TEXT, not from this row's own brief: the plan's B3 table row for I1 says tools/test.sh has 'the three announced opt-outs'; this row's own brief and the file itself name only two (EZIL_ALLOW_SKIPPED_CONTAINER_TESTS, EZIL_ALLOW_SKIPPED_BROWSER_SUITES) -- EZIL_SKIP_TYPECHECK is a third self-announcing variable in the same file but skips a typecheck stage, not a test, so CONTRIBUTING documents it separately in 'Running it locally' context rather than folding it into the skip-is-not-a-pass count. Not changed; flagged here since the plan's own wording differs from what was verified. BLIND SPOTS: (1) yaml.safe_load proves task.yml/app_proposal.yml are valid YAML, not that they satisfy GitHub's issue-form schema (dropdown/textarea shapes, required combinations) -- the real check is opening the New Issue chooser against these files once on the default branch, not done here. (2) Whether a real newcomer can follow CONTRIBUTING.md end to end is unproven; only the first genuine outside PR against these templates settles that. (3) PR #36 merged before this artifact could be updated with its number -- caught only on the advisor's post-hoc review of this row's own transcript, which is itself a process gap: an artifact write and a PR open/merge can race when checks are fast, and this row's own workflow did not serialize them.",
  "startedAt": "2026-09-04T20:28:02Z",
  "updatedAt": "2026-09-04T21:57:55Z"
}
