{
  "taskId": "I1",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "COMMITTED",
  "evidence": "PR #36 (https://github.com/EZiLHQ/ezil-os/pull/36) MERGED at 2026-09-04T21:34:24Z, squash commit b04f746, using this row's original brief's anchors (#how-to-send-a-pull-request, heading 'How big a pull request should be'). PR #38 (https://github.com/EZiLHQ/ezil-os/pull/38, branch fix/I1-anchor-contract) is OPEN, auto-merge squash ARMED 2026-09-04T21:57:50Z, NOT YET MERGED as of this writing -- do not read main's CONTRIBUTING.md as final until #38 lands; until it does, main's CONTRIBUTING.md carries the anchors #38 corrects. PROCESS GAP, stated plainly: #36's required checks went green and it auto-merged before a defect caught by post-hoc review could be fixed -- a review finding reached this artifact's evidence field only after the PR it describes had already landed. WHY #38 IS RIGHT, POSITIVE CONTROL FOUND, NOT JUST A CLAIM: `git grep -n \"CONTRIBUTING.md#\" origin/main` (after rebasing onto the tip, which now also carries row I5's tools/triage.ts, PR #35, and row I6b's docs/community/**, PR #37) shows tools/triage.ts hardcoding CONTRIBUTING.md#sign-off-dco, #how-to-send-a-pr, #reading-ci, #pr-size in its posted guidance text, AND all 22 docs/community/issues/*.md files (I6b, already merged) linking #how-to-send-a-pr and #picking-something-up -- both already written against the corrected anchor scheme before this row's fix existed, which is the positive control proving #36 was the outlier, not #38 the invention. A mid-task coordinator message independently named the same four required slugs before this grep was run; the grep is what was actually verified against, not the message's say-so. #38 renames two CONTRIBUTING.md headings so their slugs match: `## How big a pull request should be` -> `## PR size` (#pr-size), `## How to send a pull request` -> `## How to send a PR` (#how-to-send-a-pr); #sign-off-dco and #reading-ci needed no change. INBOUND-ANCHOR CHECK (not just outbound) run against origin/main post-rebase: every other file linking a CONTRIBUTING.md# anchor (.github/PULL_REQUEST_TEMPLATE.md#sign-off-dco, .github/workflows/dco.yml#sign-off-dco, README.md, docs/community/README.md, all 22 docs/community/issues/*.md, tools/triage.ts, tools/triage.test.ts) already resolves once #38's rename lands; only README.md's own link needed a matching edit (included in #38). #38 also fixes three findings from review of this row's own transcript, not from the brief: (1) tools/test.sh's header names exactly THREE rules (absent summary; unreadable failure count; any non-zero exit passed through untouched, re-read via `sed -n '28,41p' tools/test.sh`) -- #36's 'A skip is not a pass' section had substituted skip-naming for the real rule 3; skip-naming is real but lives in that file's separate 'THE SKIP HAZARD' section. Corrected to quote the real rule 3 and describe skip-naming as additional. (2) The 'PR size' prose said >400 lines 'will be asked to split' -- the landed tools/triage.ts:238 and the size/XL label description ('Over 1000 changed lines') both only trigger a split request at >1000, not ~400. Corrected to state ~400 as a preference and >1000 (size/XL) as where triage.ts actually posts the ask. (3) The Discussions section linked an issue template by raw-YAML blob path; switched to this repo's own issues/new?template=... convention (README.md already uses it for bug_report.yml). (4) 'Reading CI' named a `Vercel` check; `gh pr view 38 --json statusCheckRollup` showed the real context is `Vercel Preview Comments`, plus a `label` check (existing labeler.yml) and a plain `CodeQL` alongside the required `CodeQL (javascript-typescript)` -- none of the three are among the fifteen; the section now names all three by their real context strings rather than one loosely. VERIFY, run on the final fix/I1-anchor-contract branch: `grep -n '^## ' CONTRIBUTING.md` shows PR size / How to send a PR / Reading CI / Sign-off (DCO) all present; `python3 -c \"import yaml,sys; [yaml.safe_load(open(f)) for f in sys.argv[1:]]\" .github/ISSUE_TEMPLATE/task.yml .github/ISSUE_TEMPLATE/app_proposal.yml .github/ISSUE_TEMPLATE/config.yml` -> all three parse; `git diff --stat origin/main..HEAD` (post-rebase onto I6b's merge) shows exactly 5 files (CONTRIBUTING.md, README.md, PULL_REQUEST_TEMPLATE.md, task.yml, this artifact), no unrelated drift; `gh pr view 38 --json mergeable` -> MERGEABLE. REQUIRED-CONTEXT COMPARISON FOR #36's original content (done before #36 opened, still valid): live `gh api repos/EZiLHQ/ezil-os/rulesets/22265548 --jq '.rules[]|select(.type==\"required_status_checks\")|.parameters.required_status_checks[].context'` diffed byte-for-byte against CONTRIBUTING.md's 15 backtick-quoted lines -- empty diff, 15/15. ci.yml:64-72 fork/GHCR paragraph quoted verbatim (note: tools/triage.ts's own comment cites the same paragraph as ci.yml#L62-L72 -- a two-line labeling difference in the same block between this row and I5, neither wrong, both citing the same text; not fixed here, named as a hand-off). docs/CONTRIBUTING-APPS.md (linked from README.md and CONTRIBUTING.md) STILL does not exist -- deliberate per the brief ('row I2 is writing it in parallel -- link it anyway'); resolves once I2 merges. Labels verified live via `gh label list`, not assumed: every label referenced already exists (good first issue, help wanted, size/XS..XL, needs-triage, blocked, prereq-missing, review:claude, dependencies, all ten area labels) -- the plan's 'supervisor gh label create' step is a no-op for this row. Discussions ENABLED on the repo (`gh api repos/EZiLHQ/ezil-os --jq '.has_discussions'` -> true, measured, not assumed); only the five categories remain a founder dashboard step (no public API to create them), stated in CONTRIBUTING's Discussions section. DEVIATION FROM PLAN TEXT: plan's B3 row for I1 says tools/test.sh has 'the three announced opt-outs'; measured only two (EZIL_ALLOW_SKIPPED_CONTAINER_TESTS, EZIL_ALLOW_SKIPPED_BROWSER_SUITES) plus a third, EZIL_SKIP_TYPECHECK, that announces a skipped TYPECHECK stage, not a skipped test -- documented separately in 'Running it locally' rather than folded into the skip count. HYGIENE: the stray remote branch `task/I1` (recreated by a post-merge push before this gap was caught, holding merged content in unmerged shape) was deleted (`git push origin --delete task/I1`) so it does not read as pending work. COSMETIC HAND-OFF, not fixed here: I6b's 22 docs/community/issues/*.md files render the link TEXT as 'How to send a pull request' against the now-correct #how-to-send-a-pr anchor -- the anchor resolves, only the visible link text still says the pre-#38 phrase; whoever next touches those files can align the wording. BLIND SPOTS: (1) yaml.safe_load proves task.yml/app_proposal.yml are valid YAML, not that they satisfy GitHub's issue-form schema (dropdown/textarea shapes, required combinations) -- the real check is the New Issue chooser once these are on the default branch, not done here. (2) Whether a real newcomer can follow CONTRIBUTING.md end to end is unproven; only the first genuine outside PR against these templates settles that. (3) The #36/#38 split is itself evidence that this row's own verification loop (advisor review) ran slower than CI on a docs-only PR with all-green non-required-touching checks; a faster review pass, or holding the PR in draft until reviewed, would have caught this before merge instead of after.",
  "startedAt": "2026-09-04T20:28:02Z",
  "updatedAt": "2026-09-04T22:03:57Z"
}
