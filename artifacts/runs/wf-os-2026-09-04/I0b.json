{
	"taskId": "I0b",
	"runId": "wf-os-2026-09-04",
	"status": "done",
	"doneRung": "INDEPENDENT_TEST_PASS",
	"evidence": "THREE INCREMENTS, all measured, no brief clause refuted. (1) .gitattributes: *.ts/*.tsx/*.json/*.md text eol=lf added beside the existing *.sh/*.bash/*.mjs rules. `* text=auto`, the *.ps1 CRLF rule and all three app/public/os/* `-text` lines are untouched, and `git ls-files app/public/os/` is exactly those three byte-compared build outputs (bundle.min.js, bundle.min.css, icons.js), none of which any new glob matches. EOL COUNTS: `git ls-files --eol | grep -c i/crlf` = 0 BEFORE and 0 AFTER (i/mixed = 0 both); it is also 0 on the base commit 63eec2e itself. NO RENORMALISATION: `git add --renormalize -- '*.ts' '*.tsx' '*.json' '*.md'` stages an EMPTY diff before the change, after the change, and again after the rebase, over 321 tracked files (222 ts / 21 tsx / 43 json / 35 md), every one of which is already `i/lf w/lf`; `git status` clean throughout. `git check-attr text eol` spot-checks: telemetry.ts = text/lf, README.md = text/lf, app/package.json = text/lf, build-shell.sh = text/lf, bundle.min.js = text UNSET / eol unspecified. (2) .gitignore: `worker/worker-junit.xml`. POSITIVE CONTROL with the real file present: `git check-ignore -v` names `.gitignore:33` and `git status` does not list it. NEGATIVE CONTROLS: a sibling `worker/other-junit.xml` and a root `worker-junit.xml` both still show as `??`, so the rule is exactly as wide as the one path ci.yml writes (`bun test --reporter=junit --reporter-outfile=worker-junit.xml` under `working-directory: worker`, ci.yml:184). The file is NOT tracked (`git ls-files worker/worker-junit.xml` empty), so the ignore actually takes effect. The container leg's report stays at /tmp/container-junit.xml (ci.yml:550) and needs nothing. (3) worker/src/telemetry.ts: CONTAINER_TELEMETRY_META_RE goes from `bytes=(\\d+) inode=(\\d+)` to `bytes=\\s*(\\d+) inode=\\s*(\\d+)`. ONE functional line changed in the whole file; the rest of the diff is comments, including two stale sentences corrected (the old doc listed 'a wc that padded its output' and 'a shell without stat' as causes of the whole-parse degrade -- padding now parses, and a missing stat only degrades the inode to 0 because of the command's own `|| echo 0`). THE EMITTED COMMAND IS UNCHANGED, proved not asserted: the base file was extracted with `git show 3c76d43:worker/src/telemetry.ts` and both versions were called side by side over four argument shapes (the production path/65536, a path with a space, maxBytes 0, maxBytes 1.9) -- IDENTICAL on all four. The pinned literal in the new test is that captured base string, not one re-derived from the edited function. 9 NEW TESTS in worker/src/telemetry.test.ts (52 -> 61 in that file, 61 pass / 0 fail): the byte-for-byte command pin; a no-`tr -d`/no-`awk`/no-`sed` constraint with a positive control (`${cmd} | tr -d ' '` DOES trip it); path/byte-budget interpolation and the floor at 1; `bytes=   1234` -> 1234; the literal BSD line `bytes=       42 inode=0` -> 42; padded and bare forms producing an EQUAL result (1234 - 8 = 1226); a padded inode; and negatives -- empty, all-space, non-numeric, digit-with-an-inner-space and empty-inode fields must ALL still degrade to no-offset with the meta line left visible in `raw`, each paired with the positive control that the same shape with digits parses (offset 34), plus four sentinel/field-name strictness cases. MUTATION 1 (the parser tolerance reverted in place, `\\s*` removed from both fields): 57 pass / 4 FAIL -- exactly the four tolerance tests, by name; restored -> 61 pass / 0 fail. MUTATION 2 (the command made 'portable' with `wc -c < '${path}' | tr -d ' '`): 59 pass / 2 FAIL -- the byte-for-byte pin and the no-`tr` constraint; restored -> 61 pass / 0 fail. `git status` clean after both. FULL SUITE, `./tools/test.sh worker` from the worktree, three runs. BEFORE any edit (base 3c76d43): 1058 pass / 10 skip / 0 fail / 1068 tests / 39 files, exit 1. AFTER the edits: 1067 pass / 10 skip / 0 fail / 1077 tests / 39 files, exit 1. REBASED onto current main 63eec2e (PR #28; its 2 commits touch only docs and overlap none of this row's owns_files) and re-run: 1067 pass / 10 skip / 0 fail / 1077 tests / 39 files, exit 1. The +9 pass / +9 tests is exactly this row's new cases; 0 fail in all three. EXIT 1 IS THE BASELINE'S EXIT TOO -- tools/test.sh fails closed on skipped container tests, so the red exit code is not a regression from this row. EVERY SKIP NAMED: 9 in scripts/mobile-keyboard.container.test.ts and 1 in src/preview-timeouts.test.ts, identical before and after. The 9 are NOT an absent image -- `ezil-integrated:local` IS present on this box (4.57 GB) and the other two container suites, src/browser-sidecar.container.test.ts and src/neko-browser-window.container.test.ts, really did run against it and passed. mobile-keyboard's own printed reason is that playwright is not resolvable from that file and $PLAYWRIGHT_REQUIRE_DIR is unset. tools/test.sh nevertheless prints 'Build the image first' -- a wrong remediation hint for this skip; handed off, that file is not this row's. OTHER VERIFICATION: `bun run --cwd worker typecheck` = 0 errors on the rebased tree (note worker/tsconfig.json:17 excludes src/**/*.test.ts, so the new tests are checked by RUNNING them, not by tsc). No other consumer of the meta line exists: `ezil-telemetry-meta`/`CONTAINER_TELEMETRY_META` appears in exactly three files, all of them worker/src telemetry files. GATE HAND-OFF, MEASURED NOT GUESSED (telemetry-container-tail.test.ts is M4's file and was NOT touched). Stubs built on this Linux box: a `wc` that delegates the count and reprints it via `printf '%8d'`, and a `stat` that rejects -c with a BSD usage line on stderr and exit 1. FOUR-CELL MATRIX, 13 tests each: GNU/no stubs = 13 pass / 0 fail with either parser. wc stub only: base parser 10 pass / 3 FAIL (reproduces M4 and the real macOS leg by test name) -> fixed parser 13 pass / 0 FAIL. stat stub only: 12 pass / 1 fail with BOTH parsers (M3's original measurement, untouched by this row). Both stubs: base 10 pass / 3 fail -> fixed 12 pass / 1 fail. So after this change exactly TWO of the three gated tests could be un-gated -- '\\ud83d\\udd34 gives a re-read line the SAME eventId, and only the new lines new ids' and '\\ud83d\\udd34 keeps ids stable once the file outgrows the 64 KiB window and the tail slides'. The third, 'reports the file size and inode, and separates them from the NDJSON', still fails on a BSD userland at telemetry-container-tail.test.ts:151 `expect(Number(tail.inode)).toBeGreaterThan(0)` -> Received: 0, which is BSD `stat` having no -c, NOT `wc`. It needs a stat-only gate or that one assertion split out; the LINUX_ONLY_REASON text names both mechanisms and now only the second one is live. WHAT I AM BLIND TO: the padding tolerance is proved against a stub `wc` shaped like BSD's, not against a real macOS userland -- the PR's own macos-latest worker leg is the first real observation and it is the supervisor's to read. The .gitattributes rules change what a WINDOWS CHECKOUT WRITES, and nothing on this Linux box can observe that; the PR's windows-latest leg is that proof. I did not verify that CI's junit step still writes where the evidence step greps -- M4 measured that and nothing here moves it.",
	"startedAt": "2026-09-04T13:45:00Z",
	"updatedAt": "2026-09-04T14:30:00Z",
	"notes": "Row I0b's verify_cmd, run verbatim: `./tools/test.sh worker` = 1067 pass / 10 skip / 0 fail / 1077 tests / 39 files (exit 1, the same 9 container + 1 preview-timeouts skips the baseline had); `git ls-files --eol | grep -c i/crlf` = 0. INDEPENDENT_TEST_PASS is claimed on M4's telemetry-container-tail.test.ts -- 13 tests this worker did not write, exercising the changed parser and the pinned command -- plus the 1067-test suite over the edit. HAND-OFFS (files not in owns_files): (a) worker/src/telemetry-container-tail.test.ts -- two of its three LINUX_ONLY_REASON-gated tests can now be un-gated, measured; the survivor needs a stat-only gate or line 151 split out. (b) tools/test.sh -- its container-skip remediation says 'Build the image first' when the image IS present and the real cause is unresolvable playwright. (c) .gitattributes follow-up for T4's Windows bundle-diff gate: *.css and *.svg look free (0 CRLF, all i/lf or i/none), but *.js needs its own row -- shell/src/ezil-globals.js is i/-text because of a DELIBERATE NUL byte at offset 1406 (line 26, `const NEVER = (name) => `\\0ezil-no-fs:${name}``), so forcing `text` on it flips a file git currently protects as binary into one git will line-ending-convert; renormalisation is a no-op today (0 CR bytes) but that is a measurement, not a guarantee. (d) *.toml is the other half of M4's CRLF class (wrangler.toml is read by index.test.ts); M4's readWorkerSource() normalises it on read, so it is covered but not pinned on checkout."
}
