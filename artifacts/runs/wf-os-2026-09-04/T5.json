{
  "taskId": "T5",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "WORKER_RUNTIME_EVIDENCE",
  "evidence": "307 pass / 0 fail, 1047 expect() calls across 14 files in local/ (baseline before any edit in this worktree: 210 pass / 0 fail across 9 files; every pre-existing test still green, the one deliberate change being the golden docker-run argv, which gained the NEKO_SESSION_IMPLICIT_HOSTING pair). `bun run --cwd local typecheck` clean. `bun run --cwd local doctor` exits 1 on this machine naming the real conflict (`busy at offset 0: code:8443/tcp - set EZIL_LOCAL_PORT_OFFSET=10000`) and exits 0 with that offset set (12 pass, 2 warn, 0 fail). THE REAL-BROWSER SMOKE (local/tests/local-smoke.container.test.ts, 6 tests, real container + real Chromium): cold boot through POST /api/shell/desktop 5.7-9.1s at offset 10000; time to non-uniform pixels after the window opened 2.2-4.4s; pixel statistic over a 160x100 downsample of the decoded <video> inside the desktop iframe: samples=16000 min=11.3 max=254.3 mean=36.4 stdDev=58.8 buckets=29/32 (thresholds stdDev>=8, buckets>=3 - ~7x and ~10x margin). INPUT ORACLES, three, each with a stated blind spot: (a) GET /api/room/control has_host false -> true with host_id EZiL-PaqSM after the click (neko accepted control; blind to whether X acted); (b) xdotool getmouselocation inside the container: pointer 960,960 -> 383,268 against an expected 384,270 from a 1920x1080 stream in a 1440x810 box, tolerance 6 device px (X itself; blind to whether the application reacted) - the brief assumed xdotool was unavailable, it is at /usr/bin/xdotool; (c) /tmp/neko.log 'session host changed' (MEASURED CORRECTION: the first run asserted on docker logs and went RED - the container's stdout carries only start-neko.sh's phase lines). Also asserted: neko reports exactly 1 watching session, DockerHost.probeDisplay returns live/watching=1, and GET /api/shell/desktop?confirm=display answers {ok:true, display:'live', watching:1} - a field hardcoded 'unknown' before this row. TWO INTEGRATION DEFECTS THE BROWSER RUN FOUND, both invisible to all 210 prior tests: (1) routes.ts#isOwnDesktopOrigin pinned the desktop origin at port offset 0, so on any machine needing an offset to boot (this one - supabase-kong holds 0.0.0.0:8443) the SSRF pin rejected the host's own URL and every cold boot answered desktop_frame_foreign_origin over a healthy container ('[ezil-os:desktop] boot failed after 1639ms: desktop_unreachable (desktop_frame_foreign_origin)'); (2) main.ts#resolveHost THREW for the real path - the one function `bun run start` calls had no test at either end. REFUTED BRIEF CLAUSE (reported, not worked around): the brief and row T1's hand-off both said the desktop ignores every click unless NEKO_SESSION_IMPLICIT_HOSTING=true. The yaml half is true; the conclusion is false for the pinned image, because /usr/local/bin/start-neko.sh:3122 passes --session.implicit_hosting=true as an explicit flag and Viper ranks a flag above the environment. MEASURED THREE WAYS on ezil-os-worker-sandbox:ff199202, reading GET /api/room/settings after each boot: (A) no variable -> implicit_hosting true; (B) variable=false -> true; (C) variable=true -> true. The variable ships as a documented FALLBACK for a repointed image (env does outrank a config file - T0 measured that), every claim around it was rewritten to say so, and the doctor row was renamed from 'clicks reach the desktop' to 'implicit-hosting fallback'. MUTATIONS RED->GREEN, 7 of them: M2 the smoke's captured frame replaced by uniform grey -> 1 red naming 'stdDev below 8' (the pixel assertion, proven against the REAL run); M3 isOwnDesktopOrigin made offset-blind again -> 6 red including the smoke's 'false desktop_frame_foreign_origin' (the shipped defect reproduced); M4 resolveHost falling back to the fake -> 3 red; M5 probeDisplay's strict parse loosened so an unreadable entry counts as not-watching -> 2 red; M6 the buckets condition dropped -> 1 red (the two-tone frame); M7 the stdDev condition dropped -> GREEN at first, which EXPOSED AN UNGUARDED THRESHOLD; a NEARLY_FLAT fixture (luminance 96..120, four buckets, stdDev 6.9) was added and M7 then went red; M1 dropping NEKO_SESSION_IMPLICIT_HOSTING -> 9 red at the unit/doctor level and GREEN at the container level, which is the finding above rather than a gap - the container-level control was replaced by one that discriminates: flip implicit_hosting OFF on the LIVE container, readControlMode and the shell route both report 'manual', restore, both report 'implicit'. Measured en route: POST /api/room/settings answers 204, not 200. Cleanup: `docker ps -a | grep ezil-os-` lists NONE after the run; no browser and no server left behind.",
  "startedAt": "2026-09-04T15:05:00Z",
  "updatedAt": "2026-09-04T17:40:00Z"
}
