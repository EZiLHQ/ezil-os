{
  "taskId": "I6a",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "INDEPENDENT_TEST_PASS",
  "evidence": "tools/issues.ts (idempotent backlog publisher) + tools/issues.test.ts + fixtures/issues/{sample-issue.md, gh-issue-list.json}. ./tools/test.sh tools = 204 pass / 0 fail / 473 expect() across 4 files, typecheck clean (baseline before this row was 136 across 3 files, so triage/waves/ledger stay green; issues.test.ts alone = 68 pass / 0 fail / 143 expect()). Mutation-proof of the marker lookup: findExisting body-marker match swapped for a title match -> 9 fail / 59 pass, including 'a human-renamed issue is still found by its marker, so it is NOT re-created' (under the mutation a renamed issue re-creates -> duplicate) plus every exists/drift/run/--apply test; restored via git checkout -> 68 pass. Live DRY run against EZiLHQ/ezil-os: 22 'would create', topologically ordered (SFA-01 before DOC/FILES/IMG/PDF/SHEET-01; MCP-01 before AGENT-01), every label resolved by validateLabels against the live 29-label set, ARM-01/TAURI-01 carry 'blocked' exactly once, JSON files:22 with 0 exist / 0 drift; the dry run wrote nothing (no cursor, no docs/ change) and git status stayed clean. NEVER ran --apply.",
  "startedAt": "2026-09-04T22:24:11Z",
  "updatedAt": "2026-09-04T22:56:05Z",
  "notes": "Interpretation flagged per _MANDATORY section 3: the brief says the parser accepts 'exactly these keys' (id,title,labels,prereq,state), yet the same brief mandates a 'github: <n>' write-back into the front-matter after a create. A literal five-key parser would reject every file the moment its number is written back, bricking idempotent re-runs. Resolved by tolerating an optional 'github' key (positive integer) and failing on any other unknown key; 'github' is recorded, never used for matching (the marker is). HAND-OFFS: (1) docs/community/README.md (row I6b's file) says 'If tools/issues.ts has not merged yet, it does not exist' -- once this merges, soften that sentence. (2) DEAD-LINK finding for row I6b: all 22 issue bodies carry repo-relative links (28 total; 3 distinct targets: ../../../CONTRIBUTING.md#picking-something-up, ../../../CONTRIBUTING.md#how-to-send-a-pr, ../../CONTRIBUTING-APPS.md). Rendered verbatim (as the brief requires) into a GitHub issue body they do NOT resolve from an /issues/N URL -- they should be rewritten to absolute https://github.com/EZiLHQ/ezil-os/blob/main/... links in the source files. NOT MEASURED against a live issue render because that needs --apply. BLIND SPOTS: fresh-create GitHub search indexing lag is NOT MEASURED (PR #39 proved an already-indexed body is found by \"phrase\" in:body; a second --apply within a minute could double-create before the first is indexed -- and search API is 30/min, so 22 searches/run leaves little headroom). Deviation-not-taken: a single bulk 'gh issue list --state all --limit 200' + local marker match would be one call instead of 22 and immune to index lag, but the brief pins the per-id --search command and the verification asked to confirm that syntax, so I followed it (findExisting still does an exact full-marker substring match locally to defend against search tokenization)."
}
