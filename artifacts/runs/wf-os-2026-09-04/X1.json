{
  "taskId": "X1",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "COMMITTED",
  "evidence": "docs/research/local-agents.md landed, 536 lines, 5 sections. `grep -c 'NOT MEASURED'` = 8 (1 is the intro's definitional sentence; the other 7 are named gaps with a stated reason each: CUA's own model requirement, UI-TARS browserbase/browser-operator's remote-desktop reach, OS-Copilot's GUI-driver library + OS list + model-locality x3 summary-table cells, neko --hwenc's accepted backend values, and UI-TARS-1.5-7B's first-party VRAM number). Zero markdown [text](path) links in the doc (verified: grep -c for the pattern = 0), so the 'internal links resolve' check is vacuously satisfied by construction — every citation is either a bare code-styled repo path (verified to exist in this worktree: worker/sidecar/{contract,server,browser,redact,verbs}.mjs, worker/sidecar/README.md, worker/src/browser-sidecar.ts, mcp/src/server.ts, local/src/host/sandbox-host.ts, local/src/container/run-spec.ts, worker/scripts/start-neko.sh, docs/PLATFORM-NOTES.md, docs/ORCHESTRATION-LOG.md, SECURITY.md — all `[ -f ... ]` true) or a full/bare-domain external URL. 12 external URLs cited (grep -oE 'https?://...' = 12 distinct, plus 3 bare-domain citations for docs.nvidia.com x2, docs.docker.com x2, aicybr.com, chariotsolutions.com, openowl.dev — all fetched via WebFetch/WebSearch this session, not paraphrased from memory). Every file:line citation spot-checked against the source after a first-draft pass caught three real errors, fixed before commit: (1) the neko capture.go fallback quote was off by 3 lines (2851-2854 -> corrected to 2854-2856 after grep -n), (2) a cua Linux XTest comment was misquoted from a paraphrase into an invented direct quote (fixed to the real text at platform-linux/src/input/mod.rs:2162-2164, verified via sed), (3) Agent-S README OS-badge line numbers were guessed at 30-32 and were actually 31-33 (verified via grep -n) -- none of these changed a conclusion, all were citation-precision bugs caught by re-reading the source after writing the prose from memory of an earlier Read call. Commands run and quoted in the doc, all executed in this session: `docker --version` (29.1.3), `which nvidia-smi`/`nvidia-ctk`/`lspci` (all not found), `ls /dev/nvidia*` (no such file), `docker run --rm --entrypoint neko ghcr.io/ezilhq/ezil-neko-vscode:d74052bb-049931d7-ezil-brand8 serve --help` (unauthorized -- private GHCR package, matches T3's own 14:50Z ORCHESTRATION-LOG finding verbatim), `docker run --rm --entrypoint neko ghcr.io/m1k1o/neko/base:latest serve --help` (succeeded, digest sha256:20806497c78d..., --hwenc flag present, no backend value enumerated), `docker image inspect` for that digest, `git clone --depth 1` for trycua/cua (4446 files) and simular-ai/Agent-S into the scratch dir, `grep -rn`/`sed -n` across both clones for the file:line input-injection citations (CDP Input.dispatchMouseEvent at cua-driver-core/src/browser/pointer.rs:685, X11 XSendEvent at platform-linux/src/input/mod.rs:1041, XTest xtest_fake_input at mod.rs:2191-2196, macOS SLEventPostToPid+CGEvent::post_to_pid at platform-macos/src/input/mouse.rs:1075/1079, VNC sendMouseClick/sendKeyPress at libs/lume/src/VNC/VNCService.swift:18-19, Agent-S pyautogui.click template at gui_agents/s3/agents/grounding.py:368 and the raw exec(code[0]) sink at gui_agents/s3/cli_app.py:215), `gh search repos openowl`+`curl` to the GitHub search API (surfaced BlizzHacker/openowl as a preserved fork after the named repo 404'd -- a genuine, reportable finding, not induced). ~15 WebFetch/WebSearch calls against GitHub pages, HuggingFace, NVIDIA docs, Docker docs, and two third-party setup guides, each quoted with the exact sentence used. Three commits: 6833cbb (run artifact, running), 45fd9d5 (the document, 534 lines), 4e6b0db (citation-precision fixes, +7/-5). No code changed outside docs/research/local-agents.md and this artifact -- verified with `git diff --stat 6b93252..HEAD` touching exactly those two files. BLIND SPOTS / DEFERRED: did not clone UI-TARS-desktop, OS-Copilot, or claude-quickstarts (budget spent reading two repos deeply per the brief's 'at least two' floor rather than four shallowly); did not read neko's Go source to resolve what --hwenc actually accepts; did not attempt a GPU boot on any OS (no GPU exists on this machine, or in reach of this session, to attempt one on) -- all three named explicitly as NOT MEASURED in the doc rather than guessed.",
  "startedAt": "2026-09-04T10:07:50Z",
  "updatedAt": "2026-09-04T10:24:00Z",
  "by": "worker-sonnet-X1"
}
