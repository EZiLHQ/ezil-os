{
  "taskId": "T6",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "STATIC_CHECKS_PASS",
  "evidence": "4 commits, files owned: .github/workflows/release.yml (new, 268 lines), deploy/launcher/ezil-os.sh (new, 219 lines, bash 3.2), deploy/launcher/ezil-os.ps1 (new, 185 lines CRLF, PowerShell 5.1+), deploy/launcher/README.md (new, 104 lines). Static: python3 yaml.safe_load(release.yml) pass; bash -n on ezil-os.sh and on all 8 run: blocks extracted from release.yml pass; comment-stripped bash-4-builtin grep on ezil-os.sh clean; outbound-host-allowlist grep on both launchers clean (only ghcr.io/docs.docker.com/bun.sh/127.0.0.1 present); 2/2 action tags resolve via gh api (actions/checkout@v7, actions/attest-build-provenance@v2); pwsh confirmed NOT installed (which pwsh empty) so ezil-os.ps1 was validated by two full readings plus the same static checks, not by execution; actionlint confirmed NOT installed, substituted with the same yaml.safe_load+bash-n+gh-api method T3 used. MUTATION-PROVED (baseline GREEN -> mutant RED naming the real cause -> restored GREEN): (1) injected hostname evil.example.com into ezil-os.sh -> release.yml's static check caught it by name and failed; the check's own header comments legitimately NAME mapfile/readarray/declare-A/${var,,} to explain their absence, and the baseline run over those comments stays GREEN, proving the comment-strip is not a false-positive trap; (2) injected a real `mapfile -t x < f` usage -> caught and failed, naming the line; restored -> GREEN. (3) tag-regex: v0.2.0-rc.1 and v0.1.0 accepted; 'v1.0.0; rm -rf /' (shell metacharacter injection attempt via workflow_dispatch's tag input), an empty tag, and a tag missing the v-prefix all correctly REJECTED, exit 1, before any git/tar command runs on them. REAL BUGS FOUND AND FIXED before declaring done (not merely simulated): (A) read_env_key's pipeline exits 1 when a queried key is genuinely absent (EZIL_DESKTOP_DIGEST is absent from deploy/images.env today); under `set -o pipefail` that killed the whole script/step via `set -e` on the very first optional-key lookup - reproduced with a minimal repro script (prints 'before', never 'after'), fixed by moving `|| true` outside the pipe in both ezil-os.sh and release.yml's tarball step, re-reproduced GREEN after the fix ('after: X=' with the script continuing). This would have broken the launcher on every real machine on its first run, never caught by bash -n or any static check. (B) ezil-os.ps1: $RepoRoot was a PathInfo object from Resolve-Path, not a plain string, relying on implicit ToString() coercion at every downstream Join-Path/-ArgumentList use - hardened to .Path explicitly since pwsh is not installed here to catch a coercion surprise empirically. (C) ezil-os.ps1's failure paths only printed the stdout log, never the stderr log (Start-Process cannot safely redirect both streams to one file on Windows, so they are two files) - fixed to print both. REAL, NON-SIMULATED END-TO-END RUN on this machine: EZIL_LAUNCHER_IMAGE=ezil-os-worker-sandbox:ff199202 EZIL_LOCAL_PORT_OFFSET=10000 ./deploy/launcher/ezil-os.sh --no-browser (8443 is busy here per T5's own finding; the launcher-image override documented in deploy/launcher/README.md and both scripts' own comments was used because the GHCR packages are still private, per T3's hand-off) -> docker/bun checks PASS, image already-present detected (pull skipped), doctor 12 pass/2 warn/0 fail, host started, curl http://127.0.0.1:7080/os -> 200 (measured before AND during the run), 'Press Ctrl-C to stop' printed; SIGINT sent -> host stopped, port 7080 released (ss -ltnp empty after, was bound to bun's pid before), docker ps -a | grep ezil-os- empty both before and after (no boot was ever triggered since nothing hit /api/shell/desktop - the launcher's own scope is /os and the container lifecycle, not a full desktop boot). Negative-path real runs, each mutation-proved: docker missing from PATH -> exit 2 with the docs.docker.com pointer; bun missing from PATH -> exit 2 with the bun.sh install one-liner; no EZIL_LAUNCHER_IMAGE with the real (placeholder) deploy/images.env -> exit 2 naming the placeholder tag, an honest don't-know rather than a silent hardcoded fallback; EZIL_LOCAL_PORT=0 -> exit 2 naming why, doctor still runs first and passes since doctor.ts itself treats port 0 as valid; EZIL_DESKTOP_DIGEST correct (real sha256 digest of the local image, read from a scratch deploy/images.env) -> verified and proceeds; EZIL_DESKTOP_DIGEST wrong -> exit 2 before ever running the doctor, naming both digests. TARBALL BUILD, EXTRACTED, AND RUN FROM COLD (the check the brief's own verification list omitted, added on advisor's recommendation): built ezil-os-v0.1.0.tar.gz from this worktree (404KB; local/src+package.json+bun.lock, worker/src/{desktop-mode,screen-modes}.ts, app/public/os/*, deploy/images.env, deploy/launcher/*, LICENSE/NOTICE/ATTRIBUTIONS.md, generated RELEASE-MANIFEST.txt), extracted to a directory with zero relation to this repo and zero node_modules anywhere in it, then ran `bun run --cwd local doctor` (12 pass/2 warn/0 fail, exit 0) and `bun run --cwd local start` (real HTTP server, curl -> 200) from inside that extracted tree with NO `bun install` ever run - proves local/'s zero-runtime-dependency claim and proves worker/src/{desktop-mode,screen-modes}.ts (added to the tarball manifest beyond the row brief's literal list, since local/'s own imports need them - file:line in release.yml's own comment) resolve correctly outside the monorepo. CHANGELOG extraction (brief's required v0.2.0-rc.1 demonstration): `## [0.2.0-rc.1]` does not exist in CHANGELOG.md -> extraction step correctly FAILS (::error naming CHANGELOG.md, exit 1, empty notes file never reaches gh release create); `## [0.1.0]` exists -> extracts cleanly, 24 lines, stopping before the reference-link footer ([Unreleased]:/[0.1.0]: lines) rather than swallowing it, because the awk also stops at a reference-link-definition line (one starting with '[' and containing ']:'), not only at the next heading. SHA256SUMS: generated over the built tarball plus both launcher scripts and verified clean with `sha256sum -c`. `gh release create` itself was not run for real (would require pushing an actual tag) - named as a supervisor step, matching T3's precedent for its own registry-dependent steps. HAND-OFFS: (1) CHANGELOG.md's per-prerelease-tag heading contract is unsettled - this workflow requires a literal `## [<version>]` heading matching the tag exactly (v0.2.0-rc.1 needs '## [0.2.0-rc.1]', not a fallback to '## [0.2.0]' or '## [Unreleased]'); R1 (which owns CHANGELOG.md) needs to either add that literal heading per prerelease or ask for the extractor to be loosened - not solved here since CHANGELOG.md is outside this row's owns_files. (2) deploy/images.env's EZIL_DESKTOP_TAG placeholder means the launcher currently refuses to run without EZIL_LAUNCHER_IMAGE on every machine, including a real release tarball built from main today - this is expected and correct given T3's hand-off (row T7, still open) that nothing writes the real tag back into images.env yet; the launcher's refusal message points at EZIL_LAUNCHER_IMAGE and a release tarball rather than silently duplicating the LOCAL_DESKTOP_IMAGE_FALLBACK constant. (3) GHCR packages are still private per T3/T5's own findings - deploy/launcher/README.md's own 'One founder step this depends on' section names this explicitly; an anonymous docker pull of the real image will fail with 'unauthorized' until that flips.",
  "startedAt": "2026-09-04T11:20:00Z",
  "updatedAt": "2026-09-04T11:35:19Z"
}
