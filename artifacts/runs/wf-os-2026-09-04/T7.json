{
  "taskId": "T7",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "STATIC_CHECKS_PASS",
  "evidence": "RED FIRST: ./tools/test.sh local src/container/ (typecheck not skipped, tsc --noEmit clean) = 33 pass / 1 fail / 106 expect() calls, against a measured baseline of 34 pass / 0 fail / 109 expect() calls taken on this worktree before any edit. The 1 failure is run-spec.test.ts:335, a file OUTSIDE this row's owns_files, hardcoding the expected parseImagesEnv key set as the literal 4-element array ['EZIL_DESKTOP_IMAGE','EZIL_DESKTOP_TAG','EZIL_NEKO_IMAGE','EZIL_NEKO_TAG'] -- adding the required EZIL_NEKO_OVERLAY_TAG key (a real, mandatory part of this row) makes that array stale. NOT fixed here (not owned); parseImagesEnv itself needed no change and does not reject unknown keys (confirmed from its own doc comment: 'Unknown keys are kept'), and nothing in local/ (resolveDesktopImage, readAndResolveDesktopImage) reads EZIL_NEKO_OVERLAY_TAG -- only EZIL_DESKTOP_IMAGE/_TAG are consumed, confirmed by reading run-spec.ts end to end, so local mode's runtime behaviour is unaffected by the new key. HAND-OFF, exact two-line fix: local/src/container/run-spec.test.ts:334 title 'the four pinned keys' -> 'the five pinned keys'; line 335-337's array needs 'EZIL_NEKO_OVERLAY_TAG' inserted between 'EZIL_NEKO_IMAGE' and 'EZIL_NEKO_TAG' (sort position confirmed empirically from the diff output, O<T). 5 commits, 3 files owned, all three T3 hand-offs closed: deploy/images.env (new EZIL_NEKO_OVERLAY_TAG key = EZIL_NEKO_TAG + '-ezil-brand8', stale T3 'measured discrepancy' comment replaced with the resolved pairing rule, EZIL_DESKTOP_TAG write-back path documented with the exact gh run download command, placeholder value left byte-identical), .github/workflows/image.yml (neko-base's 'Load image pins' now greps EZIL_NEKO_OVERLAY_TAG from deploy/images.env instead of the removed workflow-level env NEKO_OVERLAY_SUFFIX; two new guards beyond the brief's ask -- empty-key check and a derivation check that EZIL_NEKO_OVERLAY_TAG is prefixed by EZIL_NEKO_TAG+'-', both advisor-caught gaps in the original consistency check; desktop job gains 'Write published-images.env' + 'Upload published-images.env' (actions/upload-artifact@v4) steps and an expanded job summary, all publishing the EZIL_DESKTOP_TAG=<sha8> pin without workflow write access), docker/neko/pins.env (NEKO_BASE_IMAGE pinned by digest: ghcr.io/m1k1o/neko/base@sha256:20806497c78de64700cb7befa674dbdc6bc4e8f0848a3dc911f0878a6726fa36, commented with run 33858675382 and 2026-09-04). Static: python3 yaml.safe_load(image.yml) pass; bash -n on all 12 run: blocks (extracted via a python/yaml script into scratch files) pass, up from T3's 11 (one new step added); bash -n docker/neko/build.sh pass (unchanged); 7/7 action tags resolve via gh api, including the new actions/upload-artifact@v4. Digest measured, not assumed: gh api .../jobs/100977792870/logs (run 33858675382's neko-base job) shows 'Digest: sha256:20806497...' on its docker pull of ghcr.io/m1k1o/neko/base:latest; docker buildx imagetools inspect of the same tag run fresh today returns the IDENTICAL index digest (:latest had not moved since that CI run) -- both the CI-recorded value and today's live measurement agree, so the pin is the digest CI actually used, confirmed still current. Positive control: docker pull ghcr.io/m1k1o/neko/base@sha256:20806497... on this box, exit 0 (image removed after); docker/neko/build.sh --dry-run prints the pinned digest reference in its plan and exits 0. build.sh needs no change: it neither parses nor validates NEKO_BASE_IMAGE (only NEKO_SHA/NEKO_APPS_SHA get a regex check), passing it through verbatim to `docker pull` and to neko-apps' own `--base_image`; verified against a real m1k1o/neko-apps@049931d7 checkout (the exact pinned SHA, confirmed via git rev-parse) that --base_image is forwarded unparsed into a plain `ARG BASE_IMAGE` / `FROM $BASE_IMAGE` in vscode/Dockerfile with no ':' splitting anywhere on that variable. Shell simulation (required by brief): extracted 'Load image pins' step run against the real worktree files from the repo root -> computed overlay_ref=ghcr.io/ezilhq/ezil-neko-vscode:d74052bb-049931d7-ezil-brand8 exactly, exit 0, matching worker/Dockerfile's ARG. THREE mutation-proved negative controls, each with its own distinct message: (1) overlay tag suffix mutated (brand8->brand9) in a scratch copy -> Dockerfile-consistency check fails with 'does not match the overlay ref this workflow computed', exit 1; (2) EZIL_NEKO_OVERLAY_TAG deleted from a scratch copy -> new empty-key guard fails with 'has no EZIL_NEKO_OVERLAY_TAG', exit 1 (this guard did not exist before this row); (3) base tag bumped (NEKO_SHA + EZIL_NEKO_TAG) while EZIL_NEKO_OVERLAY_TAG and Dockerfile ARG both left on the OLD base -> new derivation guard fires BEFORE the Dockerfile check is even reached, with 'is not aaaaaaaa-049931d7-<suffix> - the overlay derives from the base tag', exit 1 (this is the advisor-caught gap: without it, a base bump with a stale overlay would have passed both the old base-pins check and the old Dockerfile-consistency check, since post-bump they'd just be comparing two independent strings, no longer checking the derivation). 5th commit, advisor-review fixes: pins.env's digest comment had a self-contradicting closing sentence (claimed ':latest has since moved again upstream' directly after stating the CI-recorded digest and a fresh imagetools inspect agree) -- corrected to state what was actually measured (no move observed; it CAN move at any time, which is the real reason to re-check before assuming a future rebuild matches); published-images.env now also carries a '# digest: <sha256>' comment line (steps.build.outputs.digest) so a downstream consumer of the artifact alone, not just the job summary, can verify it pinned the right bytes, not just the right tag; dropped the unverified retention-days: 90 from the upload-artifact step (non-load-bearing -- the same pin lives permanently in the job summary regardless). Re-verified after the 5th commit: yaml.safe_load pass; all 12 run: blocks re-extracted and bash -n'd, 12/12 pass; 'Load image pins' step re-run against the real worktree files, unchanged output (that step's text did not change in commit 5), overlay_ref computed correctly, exit 0; bun test local/src/container/ unchanged at 33 pass / 1 fail (same single hand-off). grep -c OVERLAY deploy/images.env .github/workflows/image.yml -> 7 and 12 respectively (row's verify_cmd). Blind spot named: pins.env is under docker/neko/**, so this row's own commit sets the rebuild gate's request=true on the next push, but the existence probe then finds both base_ref and overlay_ref tags already published in GHCR (unchanged bytes, same tags) and rebuild_base is not set, so the gate correctly returns build=false -- meaning the digest-pinned base is NOT actually rebuilt/repushed by CI from this change alone; the digest pin only takes effect the next time someone bumps NEKO_SHA/NEKO_APPS_SHA or explicitly sets rebuild_base=true. GHCR push, cosign sign, and the new upload-artifact step can only be proven by the workflow running on GitHub after merge -- named as a supervisor step, not run here (same boundary T3 recorded). worker/Dockerfile and docker/neko/build.sh confirmed untouched (git diff --stat since the first commit shows only the 3 owned files).",
  "startedAt": "2026-09-04T10:37:37Z",
  "updatedAt": "2026-09-04T10:56:32Z"
}
