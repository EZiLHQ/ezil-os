{
	"taskId": "A2",
	"runId": "wf-os-2026-09-04",
	"status": "done",
	"doneRung": "INDEPENDENT_TEST_PASS",
	"evidence": "9 commits (e06b41c 5d19862 6da275d 6051a9c bb7ce87 b084df4 350362f 0a6dd2e 1790085). SUITES: ./tools/test.sh app -> 44 files / 817 pass / 0 fail, exit 0 (baseline taken in this worktree BEFORE the first edit: 40 files / 725 pass, exit 0 -- so +4 files, +92 tests). ./tools/test.sh tools -> tsc clean + 76 pass / 0 fail, exit 0. bun test sdk/ -> 20 pass / 0 fail, exit 0 (the drift guard reads routers/*.ts; no procedure renamed). bun run --cwd app build -> exit 0 with stub env; 23 routes, /auth/confirm dynamic, /auth/invited static. With NO env the build fails at `[env] Invalid server environment variables: SUPABASE_DATABASE_URL` inside /api/shell/activity -- a PRE-EXISTING condition (no .env.local in this checkout), not introduced here. NEW FILES: app/src/server/api/trpc-access.test.ts (20), app/src/app/login/access-gate.test.ts (25), app/src/app/auth/confirm/confirm-route.test.ts (19), app/src/app/auth/invited/fragment.test.ts (25); app/src/app/login/entry-contract.test.ts 11 -> 14. MUTATIONS (each restored to green): M1 the access refusal deleted from protectedProcedure = 7 failed / 13 passed, and the refused stranger's request PROVABLY reached `select \"id\",... from \"ezil_computers\" \"computers\" where (user_id = $1 and deleted_at is null) order by slot asc limit $2` -- getOrCreateDefaultComputer's first step, whose next step on an empty result is the INSERT. Shipped behaviour: exactly one statement reaches the database, the ezil_os_access read, and the call answers FORBIDDEN. M2 a signUpWithPassword/auth.signUp( reintroduced in app/src/app/login/actions.ts = 2 failed / 12 passed. M3 a NEW file app/src/components/__scratch-signup.ts calling auth.signUp( = 1 failed / 13 passed (the sweep sees new surfaces, which is the point; it scans >40 non-test files as its positive control). M4 the /os page gate deleted = 3 failed / 22 passed in access-gate.test.ts. M5 `message: OS_ACCESS_NOT_INVITED` -> `message: access.reason` = 3 failed / 42 passed. M6 the memoisation `??=` -> `=` = 1 failed / 19 passed. MEASURED PLATFORM FACTS: NextResponse.redirect(url) answers 307, not 302 (so tests assert on the Location header and 3xx-ness). Next FAILS THE BUILD on any non-route export from a route.ts (`\"isVerifiableType\" is not a valid Route export field`), which is why confirm-link.ts exists. @supabase/ssr createBrowserClient hard-codes flowType:'pkce' (dist/module/createBrowserClient.js:34) and auth-js's _getSessionFromURL throws AuthPKCEGrantCodeExchangeError('Not a valid PKCE flow url.') on an implicit-grant fragment -- so detectSessionInUrl CANNOT pick an invite fragment up and the explicit setSession in /auth/invited is the only thing that works. auth-js GoTrueAdminApi.js:95 verbatim: 'Note that PKCE is not supported when using `inviteUserByEmail`.' NOT PROVEN HERE, DEFERRED TO R2 AGAINST THE DEPLOYED HOST: that verifyOtp's session cookies actually land on the NextResponse.redirect this handler builds, and that the whole invited-user landing works end to end. There is no .env.local in this checkout and `vercel env pull` is off-limits, so the real Supabase was never contacted; the /auth/confirm tests stub the client and therefore prove control flow only. REFINEMENT OF THE BRIEF, measured: removing the /os PAGE gate alone does not let a refused user reach the write -- protectedProcedure still answers FORBIDDEN and the page's own catch turns that into <CouldNotOpen />. The write becomes reachable when the PROCEDURE check goes (M1). The page gate's job is the correct explanation plus defence in depth, and it is pinned in source (M4).",
	"startedAt": "2026-09-04T10:36:00Z",
	"updatedAt": "2026-09-04T11:28:22Z"
}
