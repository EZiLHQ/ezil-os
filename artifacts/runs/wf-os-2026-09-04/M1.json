{
  "taskId": "M1",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "INDEPENDENT_TEST_PASS",
  "evidence": "2 commits (58abf93, ed0bbcd), worker/scripts/mobile-keyboard.container.test.ts only. bun run --cwd worker typecheck: tsc --noEmit exit 0 (clean after both commits). THREE fixes, all measured/mutation-proved. (1) VACUOUS PASSES: 5 of 8 `it` bodies did `if (!available) {console.warn('...SKIPPING, not passing'); return;}` which bun counts as a PASS. Replaced with the sibling pattern (browser-sidecar.container.test.ts:202 / neko-browser-window.container.test.ts): SKIP_REASON computed once, itIf = SKIP_REASON ? it.skip : it, no early return. MUTATION-PROVED: reintroduced one early-return body, ran EZIL_VALIDATE_IMAGE=absent:x EZIL_NEKO_IMAGE=absent:x -> 1 pass/8 skip, tools/test.sh's gate_vacuous_container_passes fired by name ('reported 1 test(s) as run while every image those suites use is absent (absent:x absent:x)'), gate_container_skips also fired, exit 1; restored -> 0 pass/9 skip/0 fail, exit 1 (gate_container_skips alone, correctly). (2) THE 8 PRE-EXISTING FAILURES -- hypothesis (b) confirmed by direct measurement: every spawned `node -e` subprocess opened with bare `require('playwright')`; playwright is deliberately never a dependency of any package.json in this repo (every sibling browser-driving file -- local/tests/local-smoke.container.test.ts, e2e/prod-mobile-keyboard.mjs, every shell/*-browser-test.mjs -- resolves it via $PLAYWRIGHT_REQUIRE_DIR instead, CI installs one to /opt/ezil-testkit). MEASURED: `cd worker && node -e \"require('playwright')\"` throws Cannot find module in <30ms, before touching Docker/Chromium; sh() only reads stdout so the crash surfaced as a parsed-{} value, exactly matching the reported shapes (Expected 4/Received undefined; Expected \"fast.com\"/Received \"\"; found.length===0) -- all 8 are this one crash. Hypothesis (a) (missing overlay) checked and refuted for the default image: `docker run --rm --entrypoint sh ezil-integrated:local -c 'ls -la /var/www/ezil-mobile.js'` -> present, 61718 bytes (bare base ezil-neko-vscode:d74052bb-049931d7 -> absent, confirming the check discriminates; brand3..brand9 all present, sizes 35775/35775/42274/48500/50749/53438/61718). Fixed by adopting the same try-then-PLAYWRIGHT_REQUIRE_DIR-fallback convention every sibling uses. RE-RUN with the fix + PLAYWRIGHT_REQUIRE_DIR=/opt/ezil-testkit/node_modules against ezil-integrated:local (as tagged on this host): 8 pass/1 fail, 12 expect() calls, ~97s -- the 1 being 'exactly ONE keyboard affordance': Expected null, Received 'ezil-kbd-btn'. DIAGNOSED FURTHER (this looked like hypothesis (c) until measured): `diff -q` between that image's /var/www/ezil-mobile.js and `git show wip/mobile-keyboard:worker/assets/neko-branding/www/ezil-mobile.js` -> BYTE-IDENTICAL, 61718 bytes both; main's own copy of that file is 53438 bytes, zero occurrences of 'ezil-kbd-btn' (removed by commit 2877bdd 'one keyboard, the client's', enlarging the CLIENT's own .fa-keyboard control instead -- exactly what this test expects). PROVENANCE CHAIN CLOSED: `git show wip/mobile-keyboard:worker/Dockerfile | grep NEKO_IMAGE` -> `ARG NEKO_IMAGE=ezil-neko-vscode:d74052bb-049931d7-ezil-brand9` (61718B, wip's content); main's worker/Dockerfile:85 -> `ARG NEKO_IMAGE=ghcr.io/ezilhq/ezil-neko-vscode:d74052bb-049931d7-ezil-brand8` (53438B, matches main's own source). wip/mobile-keyboard's tip commit (57363e7) is literally titled 'wip(mobile): keyboard work in progress -- container tests failing'. CONFIRMED by isolation: built a PERSISTENT (kept, not deleted) single-layer image `ezil-m1-mainverify:local` (FROM ezil-integrated:local, COPY in ONLY main's own ezil-mobile.js) -> re-run scored 9 pass/0 fail, 12 expect() calls, EVERY scenario including the affordance check, reproduced twice (~94-101s each run). main's actual mobile-keyboard product code is NOT broken; hypothesis (c) is REFUTED for main. Reproduction command for the supervisor: `EZIL_VALIDATE_IMAGE=ezil-m1-mainverify:local PLAYWRIGHT_REQUIRE_DIR=/opt/ezil-testkit/node_modules ./tools/test.sh worker scripts/mobile-keyboard.container.test.ts` -> 9 pass/0 fail (image kept on this host, sha256:3d234204..., Dockerfile embeds the same repro command). HAND-OFF, not a fix owned by this file: whichever process last tagged `ezil-integrated:local` on this host built it from wip/mobile-keyboard, not main -- any other row/agent on this host trusting that tag is silently measuring the WIP branch, not main; and README's 'in progress on wip/mobile-keyboard' undersells it -- main's keyboard is green 9/9, the WIP branch is the one whose own tip commit says its container tests are failing. (3) DURABILITY FIX (post-advisor-review): the crash from (2), if it recurs for any other reason (playwright resolvable to SKIP_REASON's own check but the spawned subprocess still fails), was still silently swallowed by the '?? {}'/'?? \"\"'/'?? []' fallbacks in framesFor/remoteTextAfter/the affordance script -- same misleading shape, different trigger. All three now check res.status and throw with the child's stderr before falling back. MUTATION-PROVED: broke PW_RESOLVE's require target to a nonexistent module (primary AND PLAYWRIGHT_REQUIRE_DIR fallback) while leaving the parent's own playwrightUnavailableReason() check untouched (so SKIP_REASON stays null and the suite actually attempts to run) -> 1 pass/8 fail against ezil-m1-mainverify:local, every failure naming the real cause verbatim ('the node subprocess exited 2 ... stderr: ... Cannot find module mutation-proof-does-not-exist'), never Received: undefined; restored, re-verified 9 pass/0 fail. IMAGE NAME UNIFICATION: EZIL_VALIDATE_IMAGE read first (the name every sibling *.container.test.ts reads, and the name tools/test.sh's gate_vacuous_container_passes already checked), EZIL_NEKO_IMAGE (what this file read before) kept as fallback. CAUTION flagged, not fixed (outside owns_files): deploy/images.env:92 and .github/workflows/image.yml:90,112-113 define a DIFFERENT EZIL_NEKO_IMAGE -- a bare registry path (ghcr.io/ezilhq/ezil-neko-vscode, no tag) combined with EZIL_NEKO_TAG/EZIL_NEKO_OVERLAY_TAG -- vs. this file's/tools/test.sh's full image:tag reference. Same name, two incompatible shapes; low practical risk today (separate workflow files/jobs, and a malformed leaked value would just fail docker image inspect -> honest skip, not a false pass) but worth a rename if these ever share an env scope. FINAL GATED RUNS (both required by the brief, re-confirmed after fix 3): (i) image present, no PLAYWRIGHT_REQUIRE_DIR (the literal verify_cmd, no extra env) -> 0 pass/9 skip/0 fail, exit 1, banner names 'playwright is not resolvable ... $PLAYWRIGHT_REQUIRE_DIR is unset' -- honest even with the image present. (ii) EZIL_VALIDATE_IMAGE=absent:x EZIL_NEKO_IMAGE=absent:x -> 0 pass/9 skip/0 fail, exit 1, banner names 'the image `absent:x` is not present locally'. Docker cleanup: `docker ps -a --filter name=ezil-kbd-test` empty after every run. DELTAS TO STATE EXPLICITLY (found on advisor review): test count moved 8 -> 9 (added an explicit 'booted and reached readiness' itIf as test #0, matching the sibling bootError pattern -- the CSV row and O3's log entry both say '8', this file now reports 9); the 8 old phantom PASSES are now 9 RECORDED SKIPS when the image is absent, which is what row M3 (worker suites skipping on non-Linux) will also be counting from this file going forward; and `EZIL_ALLOW_SKIPPED_CONTAINER_TESTS=1` now yields exit 0 for this file specifically, where O3 measured the vacuous-pass gate firing even WITH that flag set (`gate_vacuous_container_passes` fires regardless of the flag, but it only fires when bun records something as run-not-skipped -- now that every skip is a real skip, the container-skip gate is the only one in play and that one DOES respect the flag). Deviations from the brief: none found -- the sibling SKIP_REASON/itIf pattern, the EZIL_VALIDATE_IMAGE env name, and the overlay file path (/var/www/ezil-mobile.js) were all exactly as described.",
  "startedAt": "2026-09-04T00:00:00Z",
  "updatedAt": "2026-09-04T11:45:00Z"
}
