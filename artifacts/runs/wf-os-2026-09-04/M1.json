{
  "taskId": "M1",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "INDEPENDENT_TEST_PASS",
  "evidence": "1 commit (58abf93), worker/scripts/mobile-keyboard.container.test.ts only. bun run --cwd worker typecheck: tsc --noEmit exit 0. TWO independent defects fixed. (1) VACUOUS PASSES: 5 of 8 `it` bodies did `if (!available) {console.warn('...SKIPPING, not passing'); return;}` which bun counts as a PASS. Replaced with the sibling pattern from browser-sidecar.container.test.ts:202 / neko-browser-window.container.test.ts (SKIP_REASON computed once, itIf = SKIP_REASON ? it.skip : it, no early return). MUTATION-PROVED: reintroduced one early-return body, ran with EZIL_VALIDATE_IMAGE=absent:x EZIL_NEKO_IMAGE=absent:x -> 1 pass/8 skip, tools/test.sh's gate_vacuous_container_passes fired naming the file ('reported 1 test(s) as run while every image those suites use is absent (absent:x absent:x)'), gate_container_skips also fired, exit 1; restored -> 0 pass/9 skip/0 fail, exit 1 (gate_container_skips alone, correctly, since nothing ran vacuously). (2) THE 8 PRE-EXISTING FAILURES, diagnosed by measurement against hypotheses (a)/(b)/(c) from the brief: every spawned `node -e` subprocess (framesFor/remoteTextAfter/the affordance check) opened with a bare `require('playwright')`; playwright is deliberately never a dependency of any package.json in this repo (every sibling browser-driving file -- local/tests/local-smoke.container.test.ts, e2e/prod-mobile-keyboard.mjs, every shell/*-browser-test.mjs -- resolves it from $PLAYWRIGHT_REQUIRE_DIR instead, CI installs one to /opt/ezil-testkit). MEASURED: `cd worker && node -e \"require('playwright')\"` throws `Cannot find module 'playwright'` in <30ms, before touching Docker/Chromium; sh() only reads stdout so the crash surfaced as a parsed-{} value, exactly matching the reported shapes (`Expected 4, Received undefined`; `Expected \"fast.com\", Received \"\"`; `found.length === 0`) -- all 8 are this one crash, not 8 independent bugs. This is hypothesis (b). Image-overlay hypothesis (a) was checked and REFUTED for the default image: `docker run --rm --entrypoint sh ezil-integrated:local -c 'ls -la /var/www/ezil-mobile.js'` -> present, 61718 bytes (also checked ezil-neko-vscode:d74052bb-049931d7 bare base -> absent, confirming the overlay check itself discriminates correctly; brand3..brand9 all present at growing sizes 35775/35775/42274/48500/50749/53438/61718 bytes). Fixed (2) by adopting the same try-then-PLAYWRIGHT_REQUIRE_DIR-fallback convention every sibling uses (createRequire(join(dir,'noop.js'))('playwright')), verified directly: `node -e \"createRequire(...).('playwright')\"` against /opt/ezil-testkit/node_modules resolves clean. RE-RUN with the fix + PLAYWRIGHT_REQUIRE_DIR=/opt/ezil-testkit/node_modules: `./tools/test.sh worker scripts/mobile-keyboard.container.test.ts` against ezil-integrated:local (as built/tagged on this host) -> 8 pass / 1 fail, 12 expect() calls, 97.64s -- the 1 being 'exactly ONE keyboard affordance': `Expected: null, Received: \"ezil-kbd-btn\"`. Looked like hypothesis (c) until measured further: `diff -q` between that image's /var/www/ezil-mobile.js (docker run --rm --entrypoint sh ezil-integrated:local -c 'cat ...') and `git show wip/mobile-keyboard:worker/assets/neko-branding/www/ezil-mobile.js` -> BYTE-IDENTICAL, 61718 bytes both; main's OWN copy of that file is 53438 bytes and has zero occurrences of `ezil-kbd-btn` (removed by commit 2877bdd 'one keyboard, the client's', in favour of enlarging the CLIENT's own .fa-keyboard control -- exactly what this test expects). wip/mobile-keyboard's tip commit (57363e7) is literally titled 'wip(mobile): keyboard work in progress -- container tests failing'. CONFIRMED by isolation: built a throwaway single-layer image (FROM ezil-integrated:local, COPY in ONLY main's own ezil-mobile.js, nothing else) tagged ezil-m1-mainverify:local (removed after verification, `docker rmi` confirmed) and re-ran -> 9 pass / 0 fail, 12 expect() calls, 97.64s, EVERY scenario including the affordance check. main's actual mobile-keyboard product code is NOT broken; hypothesis (c) is REFUTED for main. This is a HAND-OFF, not a fix owned by this file: whichever process last tagged `ezil-integrated:local` on this host built it from wip/mobile-keyboard, not main -- any other row/agent on this same host trusting that tag is silently measuring the WIP branch. FINAL GATED RUNS (the two required by the brief): (i) image present, no PLAYWRIGHT_REQUIRE_DIR (the literal verify_cmd with no extra env) -> `0 pass / 9 skip / 0 fail`, exit 1, SKIPPING banner names 'playwright is not resolvable ... $PLAYWRIGHT_REQUIRE_DIR is unset' -- honest, no vacuous pass, even with the image present. (ii) EZIL_VALIDATE_IMAGE=absent:x -> `0 pass / 9 skip / 0 fail`, exit 1, SKIPPING banner names 'the image `absent:x` is not present locally (build it with...)'. IMAGE resolution unified: EZIL_VALIDATE_IMAGE (the name every sibling *.container.test.ts reads, and the name tools/test.sh's gate_vacuous_container_passes already checked) read first, EZIL_NEKO_IMAGE (what this file read before) kept as fallback. Docker cleanup: `docker ps -a --filter name=ezil-kbd-test` empty after every run; scratch verification image ezil-m1-mainverify:local removed. Deviations from the brief: none found -- the sibling SKIP_REASON/itIf pattern, the EZIL_VALIDATE_IMAGE env name, and the overlay file path (/var/www/ezil-mobile.js) were all exactly as the brief described.",
  "startedAt": "2026-09-04T00:00:00Z",
  "updatedAt": "2026-09-04T11:30:00Z"
}
