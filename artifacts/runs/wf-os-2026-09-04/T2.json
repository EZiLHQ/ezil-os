{
  "taskId": "T2",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "WORKER_RUNTIME_EVIDENCE",
  "evidence": "124 pass / 0 fail, 455 expect() calls across 5 files in local/ (49 pre-existing run-spec+shell-api held green, 15 ice, 44 docker-host unit, 16 docker-host.container); `bun run --cwd local typecheck` clean. The container suite really boots the pinned image ezil-os-worker-sandbox:ff199202 on Docker 29.1.3 at hostPortOffset=10000. MEASURED: cold boot (docker run -> authenticated neko login) 5.7s; container memory after ready 591.3MiB/8GiB; warm ensureDesktop (reuse) 75ms; restartDesktop (stop+start+ready) 7.6s; host UDP mux bound `UNCONN 127.0.0.1:62100`; neko log (ANSI-stripped) `webrtc starting epr=0-0 icelite=true icetrickle=true module=webrtc nat1to1=127.0.0.1 tcpmux=62100 udpmux=62100`; zero occurrences of checkip/ip_retrieval/amazonaws, with `webrtc starting`+`nat1to1=` asserted in the same test as the positive control. MUTATIONS RED->GREEN, 7 of them: drop NEKO_WEBRTC_NAT1TO1 -> 6 red; assertNotDefault made a no-op -> 3 red; /health body check dropped so a bare 200 is ready -> 1 red; /api/login strict parse dropped so any 200 is ready -> 2 red; mux offset applied to the host side only (the silent-media bug) -> 3 red; fetchIn reverted to `port + offset` -> 1 red (only the mux case can tell the two apart; every HTTP-port assertion stays green, which is why that case exists); each restored -> 0 fail. CONTAINER-LEVEL MUTATION: the same image booted with NO password env answers HTTP 200 with profile.is_admin true for the literal `admin` and 200 for `neko` while a wrong password is 401 -- so the suite's 401s are a fact about this host's minting, not about neko rejecting everything. Containers cleaned up: `docker ps -a` lists none of mine.",
  "startedAt": "2026-09-04T08:12:00Z",
  "updatedAt": "2026-09-04T09:00:00Z"
}
