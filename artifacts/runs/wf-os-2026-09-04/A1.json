{
	"taskId": "A1",
	"runId": "wf-os-2026-09-04",
	"status": "done",
	"doneRung": "INDEPENDENT_TEST_PASS",
	"evidence": "6 commits (201214c, 527c0af, fa0bf1f, 19ddd42, 20ae5c0, db3b11d, 9ed54fd). SUITES: ./tools/test.sh app -> 40 files / 725 pass / 0 fail, exit 0 (baseline in this worktree before the first edit: 39 files / 696 pass); ./tools/test.sh tools -> tsc clean + 76 pass / 0 fail, exit 0. os-access.test.ts alone: 29 pass. MIGRATION: `bun run --cwd app db:generate --name os_access` ran OFFLINE with no database (exit 0, wrote 0002_os_access.sql + meta/0002_snapshot.json + _journal.json idx 2); re-running plain `bun run --cwd app db:generate` says 'No schema changes, nothing to migrate' and leaves the tree clean. The RLS policy block is hand-appended (drizzle-kit emits none), matching 0000/0001. REAL POSTGRES (throwaway container, public.ecr.aws/supabase/postgres:17.6.1.140 on :55433, torn down): 0000+0001+0002 applied in order with ON_ERROR_STOP, exit 0 each; \\d ezil_os_access shows the PK, the CHECK, ezil_os_access_user_id_fkey -> auth.users(id) ON DELETE SET NULL, the policy, relrowsecurity=t. CHECK proved live: `insert ... ('Bob@Example.com')` -> ERROR violates ezil_os_access_email_lower_chk; the lower-cased insert succeeds (positive control). citext MEASURED on that image: pg_available_extensions default_version 1.6, installed_version EMPTY and no pg_extension row -- available but NOT enabled, corroborating the text+CHECK choice. SHIPPED GATE against that real database (osAccessLookup + osAccessFor, real Drizzle + postgres-js): invite mode -> alice ALLOW(invited), '  ALICE@Example.COM ' ALLOW(invited), mallory DENY(revoked), MALLORY@EXAMPLE.COM DENY(revoked), stranger DENY(not_invited), null DENY(no_email); open mode -> all six ALLOW(open). CLI end to end on that database: list on an empty table says nobody can use EZiL OS; add normalises '  Alice@Example.COM ' to alice@example.com; add on an ACTIVE row updates invited_by and says so; revoke sets revoked_at and keeps the row; revoke again reports the existing timestamp, no change; revoke of an unknown email exits 1; add on a REVOKED row lifts the revocation with created_at preserved (...272Z before and after). INVITE WIRE proved against a fake GoTrue on :55444: POST /auth/v1/invite?redirect_to=https%3A%2F%2Fos.ezil.work%2Fauth%2Fcallback, apikey + Authorization: Bearer + application/json, body {\"email\":\"carol@example.com\"}; EZIL_OS_ORIGIN='https://staging.os.ezil.work/' -> .../auth/callback with the trailing slash stripped; a 422 leaves the row written and exits 1 naming --no-invite. ENV REFUSALS measured with the variable unset: --help exits 0 printing usage; list/add/revoke exit 1 naming SUPABASE_DATABASE_URL; add exits 1 naming SUPABASE_SERVICE_ROLE_KEY and, separately, SUPABASE_URL -- in both cases AFTER the row is written, which the CLI says. redact(): the connection string is replaced verbatim, and with an empty secret list the ://user:pass@ pattern still masks it; a bare key is replaced. No secret appears in any CLI output. MUTATIONS (all restored to 29 pass): M1 revoked check `row.revokedAt !== null` -> `if (false)` = 4 failed / 21 passed; M2 `mode === 'open'` -> `if (true)` = 16 failed / 9 passed; M3 no_email denial deleted = 3 failed / 22 passed; M4 lookup filters `isNull(revokedAt)` in SQL = 1 failed / 24 passed (only the SQL-text assertion catches it -- the pg-proxy fake cannot enforce a WHERE, stated as a blind spot); M5 env `.default('invite')` -> `.default('open')` = 1 failed / 28 passed. NOT DONE (supervisor/founder): 0002 is NOT applied to hosted -- per docs/RUNBOOK.md that is a by-hand step, and this row never connected to the hosted database.",
	"startedAt": "2026-09-04T15:05:00Z",
	"updatedAt": "2026-09-04T16:20:00Z"
}
