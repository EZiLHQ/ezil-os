{
	"taskId": "A1",
	"runId": "wf-os-2026-09-04",
	"status": "done",
	"doneRung": "INDEPENDENT_TEST_PASS",
	"evidence": "9 commits (201214c 527c0af fa0bf1f 19ddd42 20ae5c0 db3b11d 9ed54fd 6749e85 fecde0e). SUITES: ./tools/test.sh app -> 40 files / 725 pass / 0 fail, exit 0 (baseline taken in this worktree before the first edit: 39 files / 696 pass / exit 0, so +1 file +29 tests). ./tools/test.sh tools -> tsc clean + 76 pass / 0 fail, exit 0. os-access.test.ts alone: 29 pass. FLAKE, NAMED AS UNNAMED: one ./tools/test.sh app run reported 1 failed / 724 passed while a Postgres container was being torn down and a bun server killed; I did not capture the test name, which is a real gap. 15 subsequent full-suite runs (including one under 8-way CPU load) all reported 725 passed, and 20 consecutive runs of os-access.test.ts alone were 29 pass each, so the flake is not in a file this row added. MIGRATION: `bun run --cwd app db:generate --name os_access` ran OFFLINE with no database (exit 0; wrote 0002_os_access.sql, meta/0002_snapshot.json, _journal.json idx 2). Re-running plain `bun run --cwd app db:generate` prints 'No schema changes, nothing to migrate' and leaves the tree clean. The RLS policy block is hand-appended (drizzle-kit emits none), matching 0000/0001. REAL POSTGRES (throwaway container public.ecr.aws/supabase/postgres:17.6.1.140 on :55433, torn down after; the hosted database was never contacted): 0000+0001+0002 applied in order under ON_ERROR_STOP, exit 0 each. \\d ezil_os_access shows the email PK, ezil_os_access_email_lower_chk, ezil_os_access_user_id_fkey -> auth.users(id) ON DELETE SET NULL ON UPDATE CASCADE, the service-role policy, relrowsecurity=t. CHECK proved live: inserting 'Bob@Example.com' by hand -> ERROR violates ezil_os_access_email_lower_chk; the lower-cased insert succeeds (positive control). citext MEASURED on that image: pg_available_extensions default_version 1.6 with installed_version EMPTY and no pg_extension row -- available but NOT enabled, corroborating the text+CHECK choice. SHIPPED GATE against that real database (osAccessLookup + osAccessFor over real Drizzle + postgres-js): invite mode -> alice ALLOW(invited), '  ALICE@Example.COM ' ALLOW(invited), mallory DENY(revoked), MALLORY@EXAMPLE.COM DENY(revoked), stranger DENY(not_invited), null DENY(no_email); open mode -> all six ALLOW(open). CLI end to end on that database: list on an empty table says nobody can use EZiL OS; add normalises '  Alice@Example.COM ' to alice@example.com; add on an ACTIVE row updates invited_by and says so; revoke sets revoked_at and keeps the row; revoke again reports the same timestamp, no change; revoke of an unknown email exits 1; add on a REVOKED row lifts the revocation with created_at preserved. INVITE WIRE proved against a fake GoTrue on :55444: POST /auth/v1/invite?redirect_to=https%3A%2F%2Fos.ezil.work%2Fauth%2Fcallback with apikey + Authorization: Bearer + application/json and body {email}; EZIL_OS_ORIGIN='https://staging.os.ezil.work/' -> .../auth/callback with the trailing slash stripped. EXIT CODES measured: 0 done; 1 refused with nothing written (no args, missing SUPABASE_DATABASE_URL, unknown email on revoke); 2 row written but email not sent (GoTrue 422, missing SUPABASE_SERVICE_ROLE_KEY, missing SUPABASE_URL) -- all five rows confirmed present in the table afterwards. --help anywhere exits 0 printing usage even with no env at all; 'revoke --help <email>' no longer reorders arguments. SECRETS: no key or connection string appears in any CLI output; redact() replaces a value verbatim, and with an empty secret list the ://user:pass@ pattern still masks a connection string. Importing invite.ts under the real 'bun test' runner executes no command (import.meta.main guard, 1 pass). MUTATIONS (each restored to 29 pass): M1 `row.revokedAt !== null` -> `if (false)` = 4 failed / 21 passed; M2 `mode === 'open'` -> `if (true)` = 16 failed / 9 passed; M3 the no_email denial deleted = 3 failed / 22 passed; M4 the lookup filtering isNull(revokedAt) in SQL = 1 failed / 24 passed (only the SQL-text assertion catches it -- the pg-proxy fake cannot enforce a WHERE; stated as a blind spot); M5 env .default('invite') -> .default('open') = 1 failed / 28 passed. NOT DONE, by design: 0002 is NOT applied to the hosted database -- per docs/RUNBOOK.md that is a by-hand supervisor/founder step, and this row never connected to it.",
	"startedAt": "2026-09-04T15:05:00Z",
	"updatedAt": "2026-09-04T16:45:00Z"
}
