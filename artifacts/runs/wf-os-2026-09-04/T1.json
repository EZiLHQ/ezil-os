{
  "taskId": "T1",
  "runId": "wf-os-2026-09-04",
  "status": "done",
  "doneRung": "INDEPENDENT_TEST_PASS",
  "evidence": "bun run --cwd local typecheck: clean. bun test local/: 135 pass / 0 fail, 501 expect() calls, 6 files (T0's 49 unchanged and still green; T1 adds 86). 6 mutations, each RED then restored GREEN: (1) LOOPBACK_BIND_ADDRESSES gains 0.0.0.0 + config binds it -> 4 red, including a REAL reachability probe that then connected on 10.60.1.4; (2) /os built with bare JSON.stringify instead of the byte-identical serializer -> 3 red (payload closes its own <script>, 4 closes in a 3-element document); (3) guacamoleRunning derived from containerState==='running' -> 1 red on a running container with a dead desktop; (3b) POST /desktop trusting ensureDesktop instead of re-reading status -> 1 red; (4) frame-probe origin pin removed -> 1 red; (5) defer -> async on the bundle script tag -> 1 red. Live run: bun run --cwd local start --fake-host on 127.0.0.1:7080 served /os with the inlined payload, the stylesheet link and both deferred scripts; all 9 /api/shell/* routes answered (session 200, desktop 200, frame/display 200, preview-url + code-preview-url 200 with the loopback origins, focus 200, telemetry 202 + NDJSON written to disk, restart 200, activity 200, screen GET/POST 200), unknown route 404 JSON, / -> 302 /os; the three bundle files served from app/public/os with correct content types and mtime-derived weak ETags (703906 / 95044 / 48841 bytes). Live POST /desktop honestly refused with desktop_unreachable because nothing was serving 8181, even though the fake host reported desktopReady:true. Server killed; nothing listening on 7080 afterwards (curl: connection refused). MEASURED against ezil-neko-vscode:d74052bb-049931d7: /etc/neko/neko.yaml ships session.implicit_hosting:false, `neko serve --help` lists --session.implicit_hosting (default true), and NEKO_SESSION_* env binds (NEKO_SESSION_FILE probe printed 'sessions file does not exist file=... module=session'; control with no env printed nothing). Two defects found by the tests' own positive controls, not by review: GET /api/shell/session shared the isNew-consuming accessor and burned the latch; the no-hostname scanner treated the '//' in 'https://' as a comment, waving through the exact thing it exists to catch. NOT proven here: anything about the Docker adapter (row T2) \u2014 every test runs against a FakeSandboxHost.",
  "startedAt": "2026-09-04T09:40:00Z",
  "updatedAt": "2026-09-04T09:42:00Z"
}
